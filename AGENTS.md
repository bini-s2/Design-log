# 작업 기록 운영 원칙

이 저장소는 공개 저장소다. 따라서 **작업·학습·창작의 사실만 기록하고, 개인이나 특정 대상을 식별할 수 있는 정보는 기록하지 않는다.** 공개해도 되는지 조금이라도 애매하면 기록하지 않는다.

## 최우선 보안 원칙

모든 커밋 메시지와 작업 로그에서 아래 정보는 금지한다.

- 사용자 본인을 포함한 모든 사람의 실명, 닉네임, 이니셜, 직함과 이름의 조합
- 동료, 강사, 지인, 클라이언트 등 특정 개인을 추정할 수 있는 표현
- 회사명, 브랜드명, 기관명, 서비스명, 클라이언트명 등 특정 조직을 식별할 수 있는 고유명사
- 인하우스 작업이나 외부 프로젝트에서 특정 회사·브랜드·담당자를 유추할 수 있는 세부 정보
- 건강, 연애, 사적 관계, 상세 위치, 연락처, 계정 정보, 인증 정보, 비공개 대화
- ChatGPT 대화에 위 정보가 명시돼 있더라도 공개 로그로 옮기지 않는다.

필요한 경우 고유명사는 `인하우스 프로젝트`, `개인 프로젝트`, `학습 과제`, `웹 작업`, `디자인 작업`처럼 식별 불가능한 일반 표현으로 추상화한다. **특정 대상을 떠올릴 수 있을 정도로 구체적이면 더 일반화하거나 기록하지 않는다.**

## 감정 배제 원칙

커밋과 로그에는 작업 사실, 변경 내용, 목적, 배운 점 중 객관적으로 설명 가능한 정보만 기록한다.

- 존경, 호감, 실망, 즐거움, 아쉬움 등 개인 감정이나 특정 대상에 대한 평가를 기록하지 않는다.
- 작업 동기가 특정 사람이나 브랜드에 대한 감정인 경우 그 동기를 삭제한다.
- `누구를 존경해서 작업했다`가 아니라 `프로젝트를 작업했다`처럼 사실 중심으로 적는다.
- 개선 이유가 필요한 경우 `가독성 개선`, `반응형 대응`, `정보 구조 정리`, `작업 흐름 단순화`처럼 작업 목적만 남긴다.

## 저장소 선택

- 특정 프로젝트에 속하지 않는 디자인 작업, UX/UI 공부, 프론트엔드 공부, AI·자동화 실험은 Design-log에 기록한다.
- 프로젝트에 속한 작업은 해당 프로젝트 저장소에 상세 기록한다.
- Design-log에는 필요할 때 프로젝트명을 쓰지 않고 `프로젝트 작업`, `웹 작업`, `인하우스 작업`처럼 비식별 요약만 남긴다.

프로젝트 작업의 상세 내용 역시 위 보안 원칙을 그대로 따른다. 프로젝트 저장소 자체가 공개되어 있어도 사람·브랜드·기관·클라이언트 관련 정보는 커밋 메시지와 작업 로그에 새로 적지 않는다.

## ChatGPT 기록 트리거

다음 표현은 별도 확인 없이 현재 작업 맥락을 정리해 기록하는 명령으로 취급한다.

- `/log`
- `기록해줘`
- `깃허브에 남겨줘`
- `오늘 작업 기록해줘`
- 같은 의미의 명시적인 기록 요청

또한 사용자가 별도로 지시하지 않아도, 한 작업 세션에서 **명확한 결과물이나 학습 마일스톤이 완성됐고 공개 기록으로 적절한 경우**에는 작업 로그를 남길 수 있다. 단, 기록 전 현재 대화의 실명·브랜드명·기관명·프로젝트 고유명사·감정 표현을 제거하고 비식별화한다.

## 기록 형식

- 새 자동 기록은 기본적으로 `logs/YYYY/MM/YYYY-MM-DD.md`의 하루 로그에 추가한다.
- 같은 날 여러 작업이 있으면 새 파일을 만들지 않고 시간별 항목으로 이어 붙인다.
- 항목에는 `제목 / 카테고리 / 객관적 요약 / 필요 시 기술적 배운 점`만 남긴다.
- 과도한 회고나 대화 전문은 저장하지 않는다.
- 링크가 특정 사람·브랜드·기관을 직접 식별시키거나 비공개 정보를 노출할 수 있으면 기록하지 않는다.
- 기존 기록은 보존하되 앞으로의 기록부터 본 규칙을 최우선으로 적용한다.
- README의 Recent Activity는 최신 로그를 자동으로 갱신한다.

## 기록 품질 기준

- 실제로 완료·변경·학습한 내용만 기록한다.
- Framer/Figma 등 외부 도구의 변경은 직접 확인했거나 사용자가 전달한 사실만 적는다.
- AI가 제안한 내용과 사용자가 실제로 적용한 내용을 구분한다.
- 동일한 작업을 중복 기록하지 않는다.
- 빈 커밋, 잔디만 채우기 위한 커밋, 과거 날짜로 소급한 커밋은 만들지 않는다.
- 비식별화보다 기록의 구체성이 우선하지 않는다. **보안과 익명성이 항상 최우선이다.**

## 커밋 규칙

- 일반 작업 기록: `log: add work activity`
- 학습 기록: `log: add learning activity`
- 자동화 설정/수정: `chore: update automation`
- 문서 구조 변경: `docs: update documentation`
- 특정 프로젝트·사람·회사·브랜드명을 커밋 메시지에 넣지 않는다.
- 감정, 평가, 관계 맥락을 커밋 메시지에 넣지 않는다.

## ChatGPT 커밋 알림

ChatGPT가 직접 커밋을 남긴 경우, 작업 완료 답변에서 반드시 **`지피티가 커밋을 남겼어.`**라고 명시한다.

그 아래에 다음 내용을 짧게 알려준다.

- 커밋 메시지
- 어떤 파일 또는 기록을 변경했는지
- 실제로 무엇을 기록하거나 수정했는지에 대한 비식별 요약

여러 커밋을 남긴 경우에는 각 커밋을 구분해서 알려준다. 알림 내용에도 위 보안 원칙과 감정 배제 원칙을 동일하게 적용하며, 실명·브랜드명·기관명·특정 대상을 유추할 수 있는 정보는 다시 노출하지 않는다.

기록 후에는 원격 저장소 반영 여부를 확인하고 사용자에게 무엇을 기록했는지 비식별 형태로 짧게 알려준다.
